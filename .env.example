# Copy to .env.local (never commit real secrets)

# --- Store admin (dashboard at /admin — password login, not Supabase Auth) ---
# Generate long random strings, e.g. openssl rand -hex 32
ADMIN_PASSWORD=change-me-to-a-long-secret
ADMIN_SESSION_SECRET=change-me-to-another-long-secret

# --- Supabase (https://supabase.com → New project → Settings → API) ---
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key
# Server-only: Dashboard → Project Settings → API → service_role (never expose in browser)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# After creating the `products` table (see supabase-schema.sql), "Add product"
# in Admin will insert rows. The shop reads via anon key + RLS select policy.

# --- Vercel ---
# 1. Push repo to GitHub/GitLab.
# 2. vercel.com → Add New Project → import repo → Framework Preset: Next.js.
# 3. Environment Variables: paste the same keys as above (use Production + Preview).
# 4. Deploy. Set site URL in Supabase Auth redirect URLs when you add customer auth.

# --- Customer authentication (next steps; not wired in this demo) ---
# Option A: Supabase Auth (Email, Google) — use @supabase/ssr and server client with cookies.
# Option B: Auth.js (NextAuth) with Google + Supabase adapter — see https://authjs.dev
